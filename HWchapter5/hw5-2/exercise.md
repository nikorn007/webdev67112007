แบบฝึกหัด: Student Profile Card Layout

วัตถุประสงค์:

เข้าใจการสร้าง Element พื้นฐานและการตกแต่งด้วย CSS (Box Model, Border-radius, Shadow)

เข้าใจการใช้ position: absolute และ relative ในการจัดวาง Element ซ้อนทับกัน

เข้าใจการใช้ Flexbox เพื่อจัดเรียง Layout หลายรายการให้สวยงาม

📝 โจทย์ข้อที่ 1: สร้าง Card ต้นแบบ (Single Card Design)

คำสั่ง:
จงสร้างไฟล์ HTML ขึ้นมาใหม่ (เช่น lab_flexbox.html) และเขียนโค้ดเพื่อสร้างการ์ดข้อมูลนักศึกษา 1 ใบ โดยมีข้อกำหนดดังนี้:

โครงสร้าง (HTML):

สร้าง div ที่มี class ชื่อ student-card

ภายในประกอบด้วย:

img สำหรับรูปโปรไฟล์ (ให้หา URL รูปจากอินเทอร์เน็ต)

ส่วนเนื้อหา (div class="card-content") ที่ประกอบด้วย ชื่อ-นามสกุล (h3), คณะ (p), และเกรดเฉลี่ย (div หรือ span)

การตกแต่ง (CSS):

กำหนดให้ Card มีความกว้าง 280px

ใส่ขอบโค้งมน (border-radius) ขนาด 12px

ใส่เงา (box-shadow) ให้ดูมีมิติ

สำคัญ: รูปภาพต้องกว้างเต็ม Card (width: 100%) และไม่ล้นออกนอกขอบโค้งของ Card (ใบ้: ต้องใช้ overflow แบบไหน?)

ตกแต่งฟอนต์และระยะห่าง (Padding/Margin) ให้สวยงามตามความเหมาะสม

ผลลัพธ์ที่คาดหวัง: ได้การ์ด 1 ใบ แสดงข้อมูลครบถ้วน รูปไม่เบี้ยว และขอบโค้งมนสวยงาม

📝 โจทย์ข้อที่ 2: เพิ่มป้ายสถานะ (Status Badge with Positioning)

คำสั่ง:
จากโค้ดในข้อที่ 1 ให้นักศึกษาเพิ่มฟีเจอร์ "ป้ายแสดงสถานะ" ที่มุมขวาบนของการ์ด โดยมีเงื่อนไขดังนี้:

HTML: เพิ่ม Element span หรือ div ที่มี class status-badge เข้าไปใน student-card

CSS Position:

กำหนดให้ป้ายสถานะลอยอยู่ที่ มุมขวาบน ของการ์ด (ห่างจากขอบบนและขวาอย่างละ 15px)

คำถามชวนคิด: ต้องกำหนด position ให้กับ .student-card เป็นอะไร? และ .status-badge เป็นอะไร? เพื่อให้ป้ายอ้างอิงตำแหน่งจากตัวการ์ด ไม่ใช่ทั้งหน้าจอ

Color Logic (Class Variations): สร้าง Class เพิ่มเติมสำหรับสีพื้นหลังป้ายสถานะ ดังนี้:

.status-normal: สีเขียว (สำหรับเกรด 2.00 ขึ้นไป)

.status-probation: สีส้ม (สำหรับเกรดต่ำกว่า 2.00)

.status-retired: สีแดง (สำหรับเกรดต่ำกว่า 1.50)

ผลลัพธ์ที่คาดหวัง: มีป้ายสถานะ "ปกติ/วิทยาทัณฑ์/พ้นสภาพ" ติดอยู่ที่มุมขวาบนของรูปภาพในการ์ดอย่างถูกต้อง

📝 โจทย์ข้อที่ 3: จัดเรียงการ์ดหลายใบ (Flexbox Layout)

คำสั่ง:
ให้นักศึกษาทำการ Copy โค้ด student-card จากข้อที่ 2 ให้มีจำนวนทั้งหมด 4 ใบ โดยเปลี่ยนข้อมูล (ชื่อ, รูป, คณะ, เกรด) ให้แตกต่างกัน เพื่อให้เห็นสถานะที่หลากหลาย (ปกติ, วิทยาทัณฑ์, พ้นสภาพ)

จากนั้นให้เขียน CSS เพื่อจัดเรียงการ์ดทั้งหมด โดยมีข้อกำหนดดังนี้:

Container: สร้าง div class card-container ครอบการ์ดทั้ง 4 ใบไว้

Flexbox:

กำหนดให้ card-container แสดงผลแบบ Flexbox (display: flex)

ทำให้การ์ดเรียงตัวต่อกันในแนวนอน

การจัดการพื้นที่: หากหน้าจอแคบลง หรือพื้นที่ไม่พอ ให้การ์ดปัดลงมาบรรทัดใหม่โดยอัตโนมัติ (flex-wrap)

จัดระยะห่างระหว่างการ์ด (gap) ให้ห่างกัน 20px

จัดให้กลุ่มการ์ดอยู่กึ่งกลางหน้าจอ (justify-content)

ผลลัพธ์ที่คาดหวัง: การ์ดทั้ง 4 ใบเรียงกันสวยงาม หากย่อหน้าจอ Browser การ์ดจะต้องไหลลงมาเป็นแถวใหม่ได้ ไม่บีบอัดจนเสียรูปทรง

🚀 โจทย์เสริม (Bonus Challenge)

เพิ่มลูกเล่น hover ให้กับ .student-card: เมื่อเอาเมาส์ไปชี้ ให้การ์ดขยับลอยขึ้นเล็กน้อย (transform: translateY)

นำเข้า Google Fonts (เช่นฟอนต์ Sarabun) มาใช้เพื่อให้แสดงผลภาษาไทยสวยงาม